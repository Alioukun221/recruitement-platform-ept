/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PublicJobOfferListDto } from '../../models/public-job-offer-list-dto';

export interface GetAvailableJobOffers$Params {
}

export function getAvailableJobOffers(http: HttpClient, rootUrl: string, params?: GetAvailableJobOffers$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<PublicJobOfferListDto>>> {
  const rb = new RequestBuilder(rootUrl, getAvailableJobOffers.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<PublicJobOfferListDto>>;
    })
  );
}

getAvailableJobOffers.PATH = '/candidate/job-offers';
