/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CandidateApplicationHistoryDto } from '../../models/candidate-application-history-dto';

export interface GetMyApplications$Params {
}

export function getMyApplications(http: HttpClient, rootUrl: string, params?: GetMyApplications$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<CandidateApplicationHistoryDto>>> {
  const rb = new RequestBuilder(rootUrl, getMyApplications.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<CandidateApplicationHistoryDto>>;
    })
  );
}

getMyApplications.PATH = '/candidate/my-applications';
