/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { EvaluationResponseDto } from '../../models/evaluation-response-dto';

export interface GetMyEvaluation$Params {
  commissionId: number;
  applicationId: number;
}

export function getMyEvaluation(http: HttpClient, rootUrl: string, params: GetMyEvaluation$Params, context?: HttpContext): Observable<StrictHttpResponse<EvaluationResponseDto>> {
  const rb = new RequestBuilder(rootUrl, getMyEvaluation.PATH, 'get');
  if (params) {
    rb.path('commissionId', params.commissionId, {});
    rb.path('applicationId', params.applicationId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<EvaluationResponseDto>;
    })
  );
}

getMyEvaluation.PATH = '/commission-member/commissions/{commissionId}/applications/{applicationId}/my-evaluation';
